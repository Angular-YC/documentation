# Installation

## CLI

```bash
pnpm --filter @angular-yc/cli build
```

Or install the published package:

```bash
npm install -g @angular-yc/cli
```

## Terraform

Use `terraform-yandex-angular` and point `manifest_path` to
`deploy.manifest.json` generated by the CLI.
